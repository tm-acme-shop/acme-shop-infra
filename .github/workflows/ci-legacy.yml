name: CI (Legacy)

on:
  push:
    branches: [main, legacy]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build check
        run: echo "Build passed"
